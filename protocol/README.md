# Protocol

Protocol-level documentation for wire formats and endpoints.

- API.md (HTTP + WebSocket)

See `../spec/hydra/api.ttl` for Hydra API documentation and `../spec/telemetry.md` for usage-based telemetry.
